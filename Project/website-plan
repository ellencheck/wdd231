<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Online Language School - Site Plan</title>
  <style>
    /* Color Schema */
    :root {
      --primary-color: #2c3e50; /* Dark Blue - headings, accents */
      --secondary-color: #ecf0f1; /* Light Gray - background */
      --accent-color: #e74c3c; /* Red - buttons, highlights */
    }

    body {
      font-family: 'Roboto', sans-serif; /* Typography */
      background-color: var(--secondary-color);
      color: var(--primary-color);
      margin: 20px;
      line-height: 1.6;
    }

    h1, h2, h3 {
      color: var(--primary-color);
    }

    a {
      color: var(--accent-color);
      text-decoration: none;
    }

    section {
      margin-bottom: 30px;
      padding: 15px;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    pre {
      background-color: #f4f4f4;
      padding: 10px;
      overflow-x: auto;
    }
  </style>
</head>
<body>

  <h1>Online Language School - Site Plan</h1>

  <section>
    <h2>Site Name</h2>
    <p><strong>Online Language School</strong></p>
    <p>This name clearly represents the purpose of the website — an online platform to

